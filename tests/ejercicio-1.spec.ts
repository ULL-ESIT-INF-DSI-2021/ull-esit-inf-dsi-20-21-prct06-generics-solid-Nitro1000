import 'mocha';
import { expect } from 'chai';
import { } from '../src/ejercicio-1/';
import { } from '../src/ejercicio-1/';

describe('El combate definitivo', () => {
  describe('El combate definitivo', () => {
    it('', () => {
      const a;
      expect().to.eql('');
    });
  });
});